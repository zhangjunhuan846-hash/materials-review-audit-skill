# Inventory Agent

Create a compact map of the manuscript, figure manifest, source data inventory, database files, and reference-number files. Do not interpret scientific claims yet.

## Required output

Write a JSON packet into the `state/` directory or the corresponding CSV/report file. Keep output evidence-grounded and avoid manuscript rewriting unless requested.
