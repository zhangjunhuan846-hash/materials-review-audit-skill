# Claim Ledger Agent

Split major conclusions into atomic claims. Mark citation need, source-data need, causal overclaim, trend-only wording, low-n caution, and severity.

## Required output

Write a JSON packet into the `state/` directory or the corresponding CSV/report file. Keep output evidence-grounded and avoid manuscript rewriting unless requested.
