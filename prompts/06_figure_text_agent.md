# Figure-Text Consistency Agent

Compare text direction claims with numeric direction in source data. Flag contradictions and unsupported strong wording.

## Required output

Write a JSON packet into the `state/` directory or the corresponding CSV/report file. Keep output evidence-grounded and avoid manuscript rewriting unless requested.
