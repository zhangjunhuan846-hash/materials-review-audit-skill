# Context Pack Agent

Generate draft_map.json, selected_context.json, and evidence_pack.json. Select only claim-bearing, number-bearing, figure-bearing, and descriptor-bearing local context.

## Required output

Write a JSON packet into the `state/` directory or the corresponding CSV/report file. Keep output evidence-grounded and avoid manuscript rewriting unless requested.
