# Source Data Ledger Agent

Map every quantitative figure to required source-data files. Flag missing source data as P0/P1 depending on figure centrality. Flag orphan source-data files as P2.

## Required output

Write a JSON packet into the `state/` directory or the corresponding CSV/report file. Keep output evidence-grounded and avoid manuscript rewriting unless requested.
